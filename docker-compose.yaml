services:
  postgres:
    image: postgres:17
    container_name: prog-image-postgres
    environment:
      POSTGRES_USER: progimage
      POSTGRES_PASSWORD: progimage
      POSTGRES_DB: progimage
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: